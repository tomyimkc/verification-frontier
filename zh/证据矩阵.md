# 证据矩阵

> 本提交中的每条公开声明都映射到一个已提交、哈希绑定的工件、不可变运行 receipt 或确定性测试。
> **无支撑的声明被移除，而非弱化。** 若一条声明不在此表中，则它未被作出。
>
> 校验命令均为纯 CPU，无需 GPU。为可读性仅显示 SHA-256 前缀；完整摘要在各工件及 ZIP 的 `MANIFEST.sha256` 中。

## A. Stage A 开发运行（真实结果）

| 公开声明 | 绑定工件 / receipt | SHA-256（前缀） | 如何校验 |
|---|---|---|---|
| Pro6000 Blackwell 上一次授权的 24 家族开发运行 | `v2/artifacts/stage-a-development-result.json` → GitHub Actions 运行 `30742115988` | `6e069077…`（工件文件）；运行工件上传 `d12e366b…` | `python3 v2/build_stage_a_result.py --check` |
| 精确合并 head `1ea93128…`（PR #1813） | 同一工件 `run.mergedHeadSha` | — | `git merge-base --is-ancestor 1ea93128… origin/main` |
| 模型 Qwen2.5-7B-Instruct @ 不可变 revision `a09a3545…` | 同一工件 `model.immutableRevision` | — | `huggingface-cli repo-info`（重新接触需所有者） |
| 家族平衡 8 物理 / 8 符号 / 8 Lean | `v2/artifacts/stage-a-manifest.json` | `d93bfb60…` | `python3 v2/stage_a.py --check` |
| 23/24 JSON 解析有效 + 提案有效 | `stage-a-development-result.json` `structuredOutput` | `6e069077…` | `python3 v2/build_stage_a_result.py --check` |
| 1 条保留的格式错误 Lean 响应（`stage-a-lean-01…`，"Invalid control character"） | 同一 `structuredOutput.invalidFamilyId/invalidError/invalidResponseRetained` | — | 检查器拒绝 `invalidResponseRetained:false` |
| 2/2 开放控制保持为不可晋升弃权 | 同一 `openControlPreservation` | — | `test_stage_a_result.py` |
| 七类策略违规总计全部为零 | 同一 `policyViolationTotals` | — | 检查器拒绝任何非零总计 |
| 0 测试 / 0 审批 / 0 激活 / 0 科学结果 | 同一 `gates` | — | 检查器拒绝任何非零门 |
| 工件 ID `8831635695`，上传 SHA-256 `d12e366b…`，holder `gha-30742115988-1` 已实时释放 | 同一 `artifact` + `run` | — | GitHub Actions 工件面板（运行所有者） |

## B. 确定性验证工具

| 公开声明 | 绑定工件 | SHA-256（前缀） | 如何校验 |
|---|---|---|---|
| 150 行公开开发清单，150/150 严格校验（含 48 Lean） | `v2/artifacts/task-validation.json` | `a81cba91…` | `python3 v2/validate_task_manifest.py` |
| 30 个公开 frontier-gap 任务各绑定一次；3 个 Lean 开放控制不可晋升 | `v2/artifacts/stage-a-manifest.json` | `d93bfb60…` | `python3 v2/stage_a.py --check` |
| 收据协议：3 链 / 34 receipt / 60 blob | `v2/artifacts/receipt-rehearsal-index.json` | `df1cbc08…` | `python3 v2/build_receipt_rehearsal.py` |
| 对抗收据 benchmark：7/7 用例 | `v2/artifacts/receipt-protocol-benchmark.json` | `e0679ec0…` | `python3 v2/benchmark_receipt_protocol.py` |

## C. 确认性协议机制（仅开发）

| 公开声明 | 绑定工件 | SHA-256（前缀） | 如何校验 |
|---|---|---|---|
| CPU 协议孪生：B0–B6，8 消融，13 变体，2,160 确定性单元，0 模型/网络调用 | `v2/artifacts/protocol-twin-validation.json` | `08d991fa…` | `python3 v2/protocol_twin.py` |
| Study Root v3：756 arm + 108 B6 + 1,404 消融行；24 有效 DAG 变体；164 无效变异被拒 | `v2/artifacts/study-root-v3-validation.json` | `dce48b42…` | `python3 v2/study_root.py` + `python3 v2/benchmark_study_root.py` |
| 评分器操作特性模拟：24,000 面板 + 12 负对照（低重采样 smoke，非 power） | `v2/artifacts/scorer-operating-characteristics.json` | `dc11357a…` | `python3 v2/simulate_scorer.py` |
| 合成 rehearsal：144/144（48 Lean），明确 `confirmatoryEligible:false` | `v2/artifacts/synthetic-rehearsal-validation.json` | `0cc05525…` | `python3 v2/build_receipt_rehearsal.py` |

## D. 包完整性

| 公开声明 | 绑定工件 | 如何校验 |
|---|---|---|
| 确定性上传 ZIP + SHA-256 | `dist/GOAI-AI4R-Open-Exploration.zip` + `.sha256` | `python3 verify_bundle.py`；两次连续构建字节相同 |
| 每个公开 JSON 上声明边界被强制 | `build_bundle.py` 白名单 + `test_demo.py::test_bundle_enforces_claim_ceiling_on_every_public_json` | `python3 -m unittest test_demo.py` |

## E. 本提交明确**未**作出的声明

以下各项均为 false/缺失，且门强制如此：

- 验证器扩展获批（`verifierExtensionsApproved: 0`）
- 科学发现 / 结果（`scientificOutcome: false`）
- 已测量前沿扩展（`protocolValid: false`）
- 通用模型能力 / 能力提升（`capabilityClaim: false`）
- 竞赛成绩 / 获奖资格（`winnerLevelEligible: false`）
- AGI / ASI（`canClaimAGI: false`）
- 确认性 power / MDE（`confirmatoryPowerValidated: false`）
- 独立盲审专家评审（作者看过聚合结果——无法自计）

`candidateOnly:true`; `canClaimAGI:false`.
