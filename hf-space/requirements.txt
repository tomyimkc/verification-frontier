# Verification Frontier — HF Space dependencies.
# Uses HF Inference API (router.huggingface.co) for Qwen2.5-7B-Instruct.
# No local model loading — instant responses, zero CPU overhead.
gradio>=5.49,<6
sympy
requests
