# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Build artifacts (regenerated by build_bundle.py / build_pdfs.py)
# Keep dist/ (the committed deterministic ZIP) and the PDFs — they are the submission.
.pytest_cache/

# OS
.DS_Store
